{
  "framework": "vite",
  "buildCommand": "prisma db push --schema ../api/prisma/schema.prisma --skip-generate && npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}
