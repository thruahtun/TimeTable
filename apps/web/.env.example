# Local development only
VITE_API_URL="http://localhost:4000"

# Production on Vercel: leave VITE_API_URL unset.
# The app uses /api routes on the same domain.
# Set DATABASE_URL in Vercel → Settings → Environment Variables instead.
